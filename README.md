# homebrew-canaryai

Homebrew tap for [CanaryAI](https://github.com/jx887/canaryai) — AI agent security monitor for Claude Code.

## Install

```bash
brew tap jx887/canaryai
brew install --cask canaryai   # GUI app + CLI
brew install canaryai          # CLI only
```
